{
  "scripts": {
    "prebuild": "rm -rf dist",
    "start": "parcel src/**/*.html",
    "build": "parcel build src/**/*.html"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.11.0",
    "parcel": "^2.11.0"
  }
}
