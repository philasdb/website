{
  "scripts": {
    "prebuild": "node clean.js",
    "start": "node build-templates.js && parcel src/index.html src/**/*.html",
    "build": "node build-templates.js && parcel build src/index.html src/**/*.html"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "handlebars": "^4.7.8",
    "parcel": "^2.12.0"
  }
}
